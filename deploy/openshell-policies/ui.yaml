# UI pods (both cpg-ingester and acp-writer): cluster services + external ingress
version: 1

filesystem_policy:
  read_only: [/usr, /lib, /lib64, /etc, /opt, /bin, /sbin]
  read_write: [/sandbox, /tmp, /app, /dev, /run, /var, /home]

network_policies:
  gateway:
    name: openshell-gateway
    endpoints:
      - host: "**.svc.cluster.local"
        port: 8080
        enforcement: log
        allowed_ips:
          - "172.30.0.0/16"
          - "10.128.0.0/14"
      - host: "*"
        port: 8080
        enforcement: log
        allowed_ips:
          - "172.30.0.0/16"
          - "10.128.0.0/14"
    binaries:
      - path: "**"
  cluster_services:
    name: cluster-services
    endpoints:
      - host: "**.svc.cluster.local"
        port: 8082
        enforcement: log
        allowed_ips:
          - "172.30.0.0/16"
          - "10.128.0.0/14"
      - host: "**.svc.cluster.local"
        port: 8090
        enforcement: log
        allowed_ips:
          - "172.30.0.0/16"
          - "10.128.0.0/14"
    binaries:
      - path: "**"
