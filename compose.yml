services:

  # --- Medplum (mock-EHR FHIR server) ---

  medplum-postgres:
    image: postgres:16
    restart: unless-stopped
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: medplum
      POSTGRES_PASSWORD: medplum
    command:
      - "postgres"
      - "-c"
      - "listen_addresses=*"
      - "-c"
      - "statement_timeout=60000"
      - "-c"
      - "default_transaction_isolation=REPEATABLE READ"
      - "-c"
      - "shared_preload_libraries=pg_stat_statements,auto_explain"
    volumes:
      - medplum-postgres-data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U medplum"]
      interval: 10s
      timeout: 5s
      retries: 5

  medplum-redis:
    image: redis:7
    restart: unless-stopped
    ports:
      - "6379:6379"
    command: redis-server --requirepass medplum
    healthcheck:
      test: ["CMD", "redis-cli", "-a", "medplum", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5

  medplum-server:
    image: medplum/medplum-server:5.1.27
    restart: unless-stopped
    ports:
      - "8103:8103"
    depends_on:
      medplum-postgres:
        condition: service_healthy
      medplum-redis:
        condition: service_healthy
    command: ["env"]
    environment:
      MEDPLUM_PORT: "8103"
      MEDPLUM_BASE_URL: "http://localhost:8103/"
      MEDPLUM_APP_BASE_URL: "http://localhost:3002/"
      MEDPLUM_STORAGE_BASE_URL: "http://localhost:8103/storage/"
      MEDPLUM_DATABASE_HOST: medplum-postgres
      MEDPLUM_DATABASE_PORT: "5432"
      MEDPLUM_DATABASE_DBNAME: medplum
      MEDPLUM_DATABASE_USERNAME: medplum
      MEDPLUM_DATABASE_PASSWORD: medplum
      MEDPLUM_REDIS_HOST: medplum-redis
      MEDPLUM_REDIS_PORT: "6379"
      MEDPLUM_REDIS_PASSWORD: medplum
      MEDPLUM_BINARY_STORAGE: "file:./binary/"
      MEDPLUM_SUPPORT_EMAIL: '"CareView EHR" <support@example.com>'
      MEDPLUM_GOOGLE_CLIENT_ID: ""
      MEDPLUM_GOOGLE_CLIENT_SECRET: ""
      MEDPLUM_RECAPTCHA_SITE_KEY: ""
      MEDPLUM_RECAPTCHA_SECRET_KEY: ""
      MEDPLUM_MAX_JSON_SIZE: "5mb"
      MEDPLUM_MAX_BATCH_SIZE: "50mb"
      MEDPLUM_RATE_LIMITS_ENABLED: "false"
      MEDPLUM_BOT_LAMBDA_ROLE_ARN: ""
      MEDPLUM_BOT_LAMBDA_LAYER_NAME: "medplum-bot-layer"
      MEDPLUM_VM_CONTEXT_BOTS_ENABLED: "true"
      MEDPLUM_DEFAULT_BOT_RUNTIME_VERSION: "vmcontext"
      MEDPLUM_ALLOWED_ORIGINS: "*"
      MEDPLUM_INTROSPECTION_ENABLED: "true"
      MEDPLUM_SHUTDOWN_TIMEOUT_MILLISECONDS: "30000"
    healthcheck:
      test: ["CMD-SHELL", "node -e \"fetch('http://localhost:8103/healthcheck').then(r=>{if(!r.ok)throw 1}).catch(()=>process.exit(1))\""]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 30s

  medplum-loader:
    image: python:3.12-slim
    depends_on:
      medplum-server:
        condition: service_healthy
    environment:
      MEDPLUM_BASE_URL: "http://medplum-server:8103"
      DATA_DIR: "/data"
      SMART_CONFIG_DIR: "/config/ips-viewer"
      FHIR_ENV_DIR: "/config"
    volumes:
      - ./mock-EHR/deploy/load-medplum.sh:/load-medplum.sh:ro
      - ./mock-EHR/data:/data:ro
      - ./mock-EHR/ips-viewer/public:/config/ips-viewer
      - ./.medplum:/config
    entrypoint: ["/bin/bash", "/load-medplum.sh"]

  medplum-app:
    image: medplum/medplum-app:5.1.27
    restart: unless-stopped
    ports:
      - "3002:3000"
    depends_on:
      medplum-server:
        condition: service_healthy

  mock-ehr-app:
    build:
      context: ./mock-EHR/ui
      dockerfile: Containerfile
    ports:
      - "3000:8080"
    depends_on:
      medplum-server:
        condition: service_healthy

  ips-viewer:
    build:
      context: ./mock-EHR/ips-viewer
      dockerfile: Containerfile
    ports:
      - "3001:8080"
    depends_on:
      medplum-server:
        condition: service_healthy

  # --- Application services ---

  kogito:
    build:
      context: ./acp-writer/decision-service
      dockerfile: deploy/Containerfile
    ports:
      - "8081:8081"
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8081/q/health/ready"]
      interval: 5s
      timeout: 5s
      retries: 20

  litellm:
    build:
      context: ./platform/litellm
      dockerfile: deploy/Containerfile
    ports:
      - "4000:4000"
    env_file:
      - ./platform/litellm/deploy/.env

  mlflow:
    image: ghcr.io/mlflow/mlflow:v2.21.3
    ports:
      - "5000:5000"
    command: ["mlflow", "server", "--host", "0.0.0.0", "--port", "5000"]
    healthcheck:
      test: ["CMD", "curl", "-sf", "http://localhost:5000/health"]
      interval: 10s
      timeout: 5s
      retries: 10

  acp-writer:
    build:
      context: .
      dockerfile: acp-writer/deploy/Containerfile
    ports:
      - "8082:8082"
    depends_on:
      kogito:
        condition: service_healthy
      mlflow:
        condition: service_healthy
    environment:
      - KOGITO_URL=http://kogito:8081
      - MLFLOW_TRACKING_URI=http://mlflow:5000
      - LITELLM_URL=http://litellm:4000
      - LITELLM_API_KEY=sk-change-me
      - FHIR_SERVER_URL=http://medplum-server:8103/fhir/R4
    env_file:
      - path: .medplum/medplum-fhir.env
        required: false
    healthcheck:
      test: ["CMD", "curl", "-sf", "http://localhost:8082/health"]
      interval: 5s
      timeout: 5s
      retries: 10

  cpg-ingester:
    build:
      context: .
      dockerfile: cpg-ingester/deploy/Containerfile
    depends_on:
      acp-writer:
        condition: service_healthy
      mlflow:
        condition: service_healthy
    environment:
      - ACP_WRITER_URL=http://acp-writer:8082
      - MLFLOW_TRACKING_URI=http://mlflow:5000
    profiles:
      - ingester

  # --- cpg-ingester pod-split services ---

  cpg-ingester-ingestion:
    build:
      context: .
      dockerfile: cpg-ingester/deploy/pods/Containerfile.ingestion
    ports:
      - "8090:8080"
    healthcheck:
      test: ["CMD", "curl", "-sf", "http://localhost:8080/health"]
      interval: 5s
      timeout: 5s
      retries: 10
    profiles:
      - ingester-pods

  cpg-ingester-llm:
    build:
      context: .
      dockerfile: cpg-ingester/deploy/pods/Containerfile.llm-analysis
    depends_on:
      mlflow:
        condition: service_healthy
    environment:
      - LITELLM_URL=http://litellm:4000
      - MLFLOW_TRACKING_URI=http://mlflow:5000
    healthcheck:
      test: ["CMD", "curl", "-sf", "http://localhost:8080/health"]
      interval: 5s
      timeout: 5s
      retries: 10
    profiles:
      - ingester-pods

  cpg-ingester-assembly:
    build:
      context: .
      dockerfile: cpg-ingester/deploy/pods/Containerfile.assembly
    healthcheck:
      test: ["CMD", "curl", "-sf", "http://localhost:8080/health"]
      interval: 5s
      timeout: 5s
      retries: 10
    profiles:
      - ingester-pods

  cpg-ingester-delivery:
    build:
      context: .
      dockerfile: cpg-ingester/deploy/pods/Containerfile.delivery
    depends_on:
      acp-writer:
        condition: service_healthy
    environment:
      - ACP_WRITER_URL=http://acp-writer:8082
    healthcheck:
      test: ["CMD", "curl", "-sf", "http://localhost:8080/health"]
      interval: 5s
      timeout: 5s
      retries: 10
    profiles:
      - ingester-pods

  cpg-ingester-bff:
    build:
      context: .
      dockerfile: cpg-ingester/deploy/pods/Containerfile.bff
    ports:
      - "8095:8080"
    healthcheck:
      test: ["CMD", "curl", "-sf", "http://localhost:8080/health"]
      interval: 5s
      timeout: 5s
      retries: 10
    profiles:
      - ingester-pods

  cpg-ingester-ui:
    build:
      context: ./cpg-ingester/ui
      dockerfile: Dockerfile
    ports:
      - "3003:8080"
    healthcheck:
      test: ["CMD", "curl", "-sf", "http://localhost:8080/health"]
      interval: 5s
      timeout: 5s
      retries: 10
    profiles:
      - ingester-pods

volumes:
  medplum-postgres-data:
