# OpenAI API key (required for the default config)
OPENAI_API_KEY=sk-your-openai-key-here

# LiteLLM master key (any string — used to authenticate requests to the proxy)
LITELLM_MASTER_KEY=sk-change-me
