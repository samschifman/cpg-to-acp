"""Docling Agent — converts CPG PDF to markdown + JSON with provenance data."""

import logging

logger = logging.getLogger(__name__)


def docling_agent(state: dict) -> dict:
    logger.info("docling_agent: stub")
    return state
